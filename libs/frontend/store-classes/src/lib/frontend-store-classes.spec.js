import { frontendStoreClasses } from './frontend-store-classes';
describe('frontendStoreClasses', () => {
  it('should work', () => {
    expect(frontendStoreClasses()).toEqual('frontend-store-classes');
  });
});
